config {
  type: "declaration",
  name: "raw_events",
  database: dataform.projectConfig.defaultProject,
  schema: dataform.projectConfig.vars.bronze_dataset,
  description: "Raw events in bronze.raw_events, one partition per load_month (monthly loads)."
}